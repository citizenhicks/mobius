Preserves authoritative completion output from Responses streams, including final citation annotations and items supplied only at completion. Earlier streamed item snapshots no longer overwrite that metadata.

Malformed completion output remains an error. Stored transcripts are unchanged; missing historical citation URLs are not reconstructed.
