Adds authenticated folder attachment as extra sandbox read/write/command roots while retaining one primary workspace. Chat metadata advances to version 15 with `attached_folders`; existing version-14 chat metadata requires the offline migration before startup.

Wire protocol advances from 70 to 71 with `attach_session_folder`. Deploy this gateway before matching CLI or iOS clients. Binary packages retain cloudflared, LICENSE, and NOTICE.
