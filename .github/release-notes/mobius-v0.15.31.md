Allows sandbox file tools to use authorized absolute paths inside the primary workspace while preserving pinned-root, traversal, symlink, and outside-root protections.

Public APIs, checkpoint format 17, and protocol records are unchanged from 0.15.30.
