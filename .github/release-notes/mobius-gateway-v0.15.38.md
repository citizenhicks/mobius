Updates the runtime to mobius 0.15.38 with bounded WebSocket retries and HTTP fallback.

Git diff reads run as bounded background tasks, keeping chat requests and session events responsive. Git operations now have a 30-second timeout.

Protocol 82, checkpoint payload 17, and SQLite schema 10 are unchanged.
