Clarifies the boundary between Swarm Bots and this chat's subagent task tree. Swarm messages and replies explicitly use `swarm_post` with an exact `@handle`; subagent tools describe canonical task paths and give scoped recovery guidance for unknown targets.

Swarm participant context reinforces the correct reply route while preserving reply limits and automatic final-result sharing. Routing and tool names are unchanged.
