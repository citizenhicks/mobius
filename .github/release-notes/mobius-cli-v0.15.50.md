# möbius CLI 0.15.50

Updates the bundled gateway to 0.15.50. Local startup replaces older gateway releases, reuses equal or newer versions, and waits for startup to complete before authenticating. Existing gateway state and paired clients are preserved.

Pins mobius 0.15.47 and mobius-gateway 0.15.50. Protocol 85 and CLI configuration are unchanged.
