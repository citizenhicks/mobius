Renames the macOS menu bar app's empty-chat label to “New conversation” and updates the bundled TLS runtime to rustls 0.23.45 for RUSTSEC-2026-0285.

Wire protocol 82, gateway configuration 26, checkpoint payload 17, and SQLite schema 10 are unchanged from 0.15.30. Existing cloud gateways can upgrade in place; no state conversion is required.
