# möbius 0.15.15

Voice task extraction uses the lowest supported reasoning effort without changing the Bot's configured effort. Public Realtime sessions request minimal reasoning and near-field noise reduction. Native voice audio enables echo cancellation and noise suppression without automatic gain boosting.

Final empty transcripts now clear earlier partial text instead of preserving noise fragments. Workspace voice progress sends concise tool status instead of full tool inputs and outputs.
