# mobius-cli 0.15.20

Updates the bundled gateway and framework with backpressure for fast model streams, preventing recorder queue overflow from aborting normal model output.

Includes Linux x86_64 and macOS ARM64 packages. Gateway protocol 76 is unchanged.
