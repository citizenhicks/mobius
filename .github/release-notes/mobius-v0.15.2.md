Adds pinned writable folder roots for absolute-path file tools and workspace-isolated commands. Adds provider-neutral raster image input and a guarded `view_image` tool; text-only routes no longer expose it, and each tool batch is limited to one image.

Checkpoint format 13 is unchanged.
