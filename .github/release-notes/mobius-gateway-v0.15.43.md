Ship the GPT-6 Luna, Sol, and Astra provider catalogs and OpenAI pricing from mobius 0.15.42.

Before starting this release on an existing gateway, stop it and migrate saved 5.6 provider, Bot-default, and Bot selections to GPT-6 routes; removed model IDs cannot load from saved gateway state. Protocol 82, checkpoint payload 17, and SQLite schema 10 are unchanged.
