Updates the framework to 0.14.2 with clearer Swarm and subagent messaging guidance, including fresh participant context and actionable errors for subagent targets outside the current task tree.

Gateway protocol remains 69. This patch requires no checkpoint or session-data migration.
