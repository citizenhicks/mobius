# mobius-gateway 0.15.20

Updates the framework to prevent fast model output from aborting with “event recorder queue is full.” Streamed model events wait for the durable recorder, while parallel tools remain available.

Gateway protocol 76 and checkpoint format 14 are unchanged. Includes signed and notarized macOS app and Linux/macOS gateway packages.
