Fix CLI startup failing with `Stopped("a path led outside of the filesystem")` for linked user-installed skills.

Pins mobius 0.15.40 and mobius-gateway 0.15.41. No changes to installed skill files or CLI configuration are required.
