# möbius CLI 0.15.48

- Adopt möbius 0.15.46 and Gateway 0.15.48.
- Derive invalid-command usage from existing command metadata.
- Use one generic composer hint while capability commands remain discoverable in the command menu.
- Render the simplified `bash` and `manage_command` tools through their framework-owned presentation.
