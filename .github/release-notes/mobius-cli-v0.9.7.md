# möbius CLI 0.9.7

- Bundles möbius 0.9.8 and möbius Gateway 0.9.12 using protocol 45.
- Keeps portable extension management and host Git/SSH credential setup while removing experimental MCP connection support.

Configuration 19 is intentionally incompatible with this release. Stop the old Gateway and back up its state before installing both commands together.
