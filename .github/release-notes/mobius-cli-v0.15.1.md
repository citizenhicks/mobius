Updates the bundled gateway to 0.15.1, including provider sign-in recovery across paired-client disconnects. The CLI continues to use protocol 70 and mobius 0.15.0.

Publish and deploy gateway 0.15.1 before clients that automatically retry interrupted sign-in requests. Binary packages include both executables, pinned cloudflared, LICENSE, and NOTICE.
