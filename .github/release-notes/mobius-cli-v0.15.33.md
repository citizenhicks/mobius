Bundles möbius Gateway 0.15.32, whose Full access policy now applies consistently to file tools and shell commands.

Wire protocol 82 and stored gateway formats are unchanged. Upgrade cloud gateways and clients together; no state conversion is required.
