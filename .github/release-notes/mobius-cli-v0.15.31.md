Removes pristine empty conversations when `/resume` navigates away and wraps long diff rows instead of requiring horizontal scrolling.

Wire protocol 82 and stored gateway formats are unchanged from 0.15.30.
