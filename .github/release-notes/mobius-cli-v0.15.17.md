# möbius CLI 0.15.17

Bundles gateway 0.15.17 with less repeated configuration, catalog, and replay work, plus direct Bot voice delegation and GPT-Live 1 support for API accounts.

Uses unchanged protocol 75 and checkpoint format 14. Upgrade the CLI, gateway, and Mac companion together. Existing conversation storage needs no conversion; packages preserve LICENSE and NOTICE.
