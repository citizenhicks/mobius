Peer coordination now renders as message-received activity with sender and full text. Subagent send/follow-up events include the message content, and write_handoff events expose the saved notes through existing tool details.

Protocol 70 and runtime storage formats are unchanged.
