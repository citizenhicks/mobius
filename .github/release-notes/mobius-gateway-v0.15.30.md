Adds a bot-tinted, click-through macOS control cursor overlay and packages its pointer asset with the gateway menubar app.

Wire protocol 82, gateway configuration 26, checkpoint payload 17, and SQLite schema 10 are unchanged from 0.15.29. Existing Cloud gateways can upgrade in place; no state conversion is required.
