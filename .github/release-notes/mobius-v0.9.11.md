# möbius 0.9.11

- Accepts bounded model-stream bursts without overflowing the event recorder command path.
- Clarifies in the `read_file` tool schema that ordinary paths are workspace-relative and outside paths require configured read roots.

Gateway protocol 47, configuration 20, and chat specification 9 are supported by the companion Gateway release.
