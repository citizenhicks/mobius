Rejects unknown pre-authentication fields, retires saturated WebSocket consumers instead of buffering without bound, and includes the framework's subagent-status and audited API improvements.

Wire protocol 81, gateway configuration 26, checkpoint payload 17, and SQLite schema 10 are unchanged from 0.15.26.
