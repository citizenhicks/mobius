Updates mobius and mobius-gateway to 0.15.5, including actionable ChatGPT session-expiry errors. Terminal profile and dashboard requests omit remote subscription reads, and superseded background profile requests do not show spurious errors.

Uses gateway wire protocol 72; update the gateway before connecting this CLI. Binary packages retain both executables, cloudflared, LICENSE, and NOTICE.
