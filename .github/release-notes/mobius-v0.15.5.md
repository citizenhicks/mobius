Reports expired or invalid ChatGPT refresh credentials with a clear instruction to sign in again, without exposing provider response bodies. Normal token refresh and the bounded unauthorized retry remain in place.

Adds provider-owned subscription usage windows for Codex, including additional rate-limit buckets, validated percentages and reset times, and authenticated usage reads. Checkpoint format 13 is unchanged.
