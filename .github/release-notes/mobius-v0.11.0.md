## Highlights

- Makes human approval the default and removes automatic AI approval review.
- Gives durable Swarm participant conversations fresh, bounded shared-chat context.
- Tightens checkpoint and protocol validation for the new security boundary.

Checkpoint 13 and SQLite schema 9 are strict; migrate older state offline before startup.
