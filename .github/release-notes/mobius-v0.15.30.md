Returns realtime voice session setup as soon as SDP negotiation completes, while bounding sideband setup, preconnect buffering, and cleanup.

Public APIs, checkpoint format 17, and protocol records are unchanged from 0.15.29.
