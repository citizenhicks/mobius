# möbius Gateway 0.9.13

- Adds authenticated, bounded UTF-8 workspace file creation and replacement for compatible frontends.
- Keeps a root `.env` visible in the workspace file catalog even when Git ignores it.
- Advances the gateway protocol to 46; upgrade clients and the gateway together.
