Adds a navigable workspace diff browser, improves multi-file diff rendering, and uses the available height in session pickers.

Wire protocol 82 and stored gateway formats are unchanged from 0.15.28.
