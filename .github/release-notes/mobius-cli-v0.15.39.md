Shows the latest usable reasoning title while the agent works, with the existing working label as a fallback. The footer includes staged and unstaged Git diff summaries.

Updates the bundled gateway to 0.15.38 with streaming recovery and background Git reads. Binary packages include both executables, pinned cloudflared, LICENSE, and NOTICE.
